import { AvailableItemsDirective } from './available-items.directive';

describe('AvailableItemsDirective', () => {
  it('should create an instance', () => {
    const directive = new AvailableItemsDirective();
    expect(directive).toBeTruthy();
  });
});
